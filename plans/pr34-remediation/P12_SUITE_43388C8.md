# Complete regression at 43388c8

Implementation/test source43388c8f7298946c4a711850c25545925b58f01c. Later main
commits while the runs execute contain documentation/evidence only. Scheduler and
backpressure changes are isolated in a separate detached worktree. No native model
or UI campaign is enabled in these generic suites. No final-gate PASS.

## Windows

Exec60009 is terminal. **2328 PASS,8 FAIL,119 SKIP,1 ERROR,2 warnings1945.37s**.
Manifest generated from completed JUnit: `evidence/p12-suite-43388c8-windows.json`.
All test identities/results are retained without captured logs, credentials or
private payload. JUnit records2456 cases; its measured suite time is1944.371s.

Eight failures pin surface revision51 although the new contract is52:
test_comm_presets, test_feature_flags, test_handoff_dependencies, test_health,
test_memory, two test_replay_marker cases and test_verification. Their seven files
were corrected in the isolated worktree; the selected gate passed9 tests5.01s.
This selection does not convert the full run to PASS.

The setup ERROR in
`test_harness_routes.py::test_harness_open_claude_code_attach_requires_target_pid`
was `ready.wait(10)` expiring before the production HTTP fixture signaled startup.
No failure of the attach assertion was observed because the test did not reach it.
An unchanged main-source targeted rerun passed1 test2.89s using
`rtk proxy .venv/Scripts/python.exe -m pytest -q --tb=short tests/test_harness_routes.py::test_harness_open_claude_code_attach_requires_target_pid`.
Cause is not established by that rerun; do not relabel the original ERROR as PASS
or silently increase the readiness timeout. Full platform suites overlapped on one
host, so resource contention is a possibility, not a proven diagnosis.

Warnings: existing Starlette/httpx deprecation and the explicitly injected Pi
death-callback RuntimeError surfaced as PytestUnhandledThreadExceptionWarning.
Skipped cases retain platform/optional/native meaning; they are not blanket PASS.

Command: `python -m pytest -q --tb=short --junitxml=<unique-temporary-results.xml>`.
Wrapper explicitly emptied OKTO_NEXUS_NATIVE_CAMPAIGN and set legacy native live
flags/UI campaign0. The raw XML remains in the unique temporary directory recorded
in P12_REMAINING_GATE_AUDIT.md; no tests or models were replayed while reducing it.

## Linux

Exec87041 remains live; poll the same handle. No terminal counts yet. Do not copy
Windows counts into Linux evidence. Source freeze remains in effect on main until
this run terminates. Its result path is in P12_REMAINING_GATE_AUDIT.md.

## Subsequent development

P06 outbox-capacity gate in the isolated checkout now passed **68 tests166.66s on
Windows and68 tests203.05s on Linux**, after ensuring stdio children use that
checkout. Native harness children still have sealed environments. This is selected
development evidence for migration058, not a replacement complete suite.

Agent-level scheduler selection initially passed37 tests118.44s, but a new
behavioral regression proved that a native durable result could release the SQL
quota before its blocked transport stack returned: **1 FAIL12.20s**. The current
fix additionally excludes represented agents of actual in-flight normal calls
across command/outbox workers. Controls retain independent capacity, and accepted
inference may proceed concurrently once its transport call actually returns.
Selected Windows42902/Linux99075 gates are running in the isolated checkout.

Next: preserve Linux terminal results; finish scheduler gates; integrate, version
and retest the changes; complete safe retry/backoff/fallback and matrix/stress/
performance/build requirements. No merge or claim of full plan completion.
